// NextAuth necesita responder peticiones GET (inicio/callback) y POST (acciones).
// La carpeta [...nextauth] es una ruta dinámica que agrupa todos sus endpoints.
export { GET, POST } from '../../../../auth'
